package com.example.carservice.entity;

/**
 * Role Enum
 *
 * Defines the two official user roles within ServiceMate:
 * - CUSTOMER: Vehicle owner booking services, tracking repairs, approving estimates.
 * - SERVICE_CENTER: Workshop staff managing intake, inspections, repairs, invoicing.
 */
public enum Role {
    CUSTOMER,
    SERVICE_CENTER
}
